import { getRoutes } from './index';

describe('getRoutes', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should create some tests', () => {
    expect(true).toBe(true);
  });
}); 