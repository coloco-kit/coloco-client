import { apiCallFlat } from './index';

describe('ApiClient', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should create some tests', () => {
    expect(true).toBe(true);
  });
}); 